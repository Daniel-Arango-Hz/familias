---
import { obtenerAutores } from '../utils/api';

let autores: any[] = [];
try {
  const respuesta = await obtenerAutores();
  autores = (Array.isArray(respuesta) ? respuesta : (respuesta.data || [])).slice(0, 4);
} catch (error) {
  console.error('Error al obtener autores:', error);
}

// Mapear los datos del backend al formato esperado
const autoresFormateados = autores.map((autor: any) => ({
  nombre: autor.nombre || 'Anónimo',
  bio: autor.especialidad || 'Autor destacado',
  libros: autor.total_libros || 0,
  descargas: autor.total_descargas || 0,
  seguidores: autor.total_seguidores || 0,
  slug: autor.slug || '',
}));
---

{autoresFormateados.length > 0 && (
  <section class="section section-bg" id="autores">
    <div class="container">
      <div class="section-header section-header-row">
        <div class="section-header">
          <h2>Autores Destacados</h2>
          <p>Conoce a quienes crean contenido para bendecir tu hogar</p>
        </div>
        <a href="/autores" class="ver-mas">Ver todo →</a>
      </div>

      <div class="autores-grid">
        {autoresFormateados.map((autor) => (
          <article class="autor-card">
            <a href={`/autor/${autor.slug}`} class="autor-card-link" aria-label={`Ver perfil de ${autor.nombre}`}>
              <div class="autor-avatar" aria-hidden="true">
                <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#9aabb8" stroke-width="1.5" aria-hidden="true">
                  <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
                  <circle cx="12" cy="7" r="4"/>
                </svg>
              </div>
              <h3 class="autor-nombre">{autor.nombre}</h3>
              <p class="autor-bio">{autor.bio}</p>
              <hr class="autor-divider" />
              <div class="autor-stats">
                <div class="autor-stat">
                  <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
                    <rect x="3" y="4" width="13" height="16" rx="2"/>
                    <path d="M7 8h5M7 12h5M7 16h3" stroke-linecap="round"/>
                  </svg>
                  <strong>{autor.libros}</strong>
                  <span>Libros</span>
                </div>
                <div class="autor-stat">
                  <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
                    <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
                    <polyline points="7 10 12 15 17 10"/>
                    <line x1="12" y1="15" x2="12" y2="3"/>
                  </svg>
                  <strong>{autor.descargas.toLocaleString('es')}</strong>
                  <span>Descargas</span>
                </div>
                <div class="autor-stat">
                  <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
                    <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
                    <circle cx="9" cy="7" r="4"/>
                    <path d="M23 21v-2a4 4 0 0 0-3-3.87M16 3.13a4 4 0 0 1 0 7.75"/>
                  </svg>
                  <strong>{autor.seguidores}</strong>
                  <span>Seguidores</span>
                </div>
              </div>
            </a>
          </article>
        ))}
      </div>
    </div>
  </section>
)}

<style>
  .autores-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.5rem;
  }

  .autor-card {
    background: var(--color-white);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-sm);
    transition: box-shadow 0.25s, transform 0.25s;
  }

  .autor-card:hover {
    box-shadow: var(--shadow-md);
    transform: translateY(-3px);
  }

  .autor-card-link {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 1.75rem 1.25rem 1.5rem;
    gap: 0.6rem;
  }

  .autor-avatar {
    width: 72px;
    height: 72px;
    border-radius: 50%;
    border: 2px solid var(--color-cream-dark);
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-cream);
    margin-bottom: 0.25rem;
  }

  .autor-nombre {
    font-family: var(--font-serif);
    font-size: 1.05rem;
    font-weight: 700;
    color: var(--color-navy);
  }

  .autor-bio {
    font-size: 0.85rem;
    color: var(--color-text-muted);
    line-height: 1.5;
  }

  .autor-divider {
    width: 100%;
    border: none;
    border-top: 1px solid var(--color-border);
    margin: 0.25rem 0;
  }

  .autor-stats {
    display: flex;
    gap: 1.25rem;
    justify-content: center;
    width: 100%;
    padding-top: 0.25rem;
  }

  .autor-stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.2rem;
    color: var(--color-gold);
  }

  .autor-stat strong {
    font-size: 1rem;
    font-weight: 700;
    color: var(--color-navy);
    line-height: 1;
  }

  .autor-stat span {
    font-size: 0.75rem;
    color: var(--color-text-muted);
  }

  @media (max-width: 1024px) {
    .autores-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 480px) {
    .autores-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
